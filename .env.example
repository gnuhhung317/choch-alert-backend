# Binance API (optional, can use without for public data)
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_SECRET=your_binance_secret_here

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_chat_id

# Trading Configuration
SYMBOL=BTCUSDT
TIMEFRAMES=5m,15m,30m,1h

# Pivot Configuration
PIVOT_LEFT=1
PIVOT_RIGHT=1
KEEP_PIVOTS=200

# Variant Filters (1=enabled, 0=disabled)
USE_VARIANT_FILTER=1
ALLOW_PH1=1
ALLOW_PH2=1
ALLOW_PH3=1
ALLOW_PL1=1
ALLOW_PL2=1
ALLOW_PL3=1

# Flask Configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=0

# Data Fetching
HISTORICAL_LIMIT=500
UPDATE_INTERVAL=60

# ===== CHART CONFIGURATION (NEW) =====
# 
# Có 2 modes:
# 1. HISTORY MODE - Vẽ chart toàn bộ lịch sử (chạy 1 lần rồi thoát)
#    - Load toàn bộ dữ liệu lịch sử
#    - Vẽ chart đàng hoàng với tất cả pivots
#    - Lưu charts vào thư mục 'charts/'
#    
# 2. REALTIME MODE (default) - Chạy liên tục, gửi alert Telegram
#    - Quét data theo interval của từng timeframe
#    - 15m timeframe: quét 15 phút 1 lần
#    - 1h timeframe: quét 60 phút 1 lần
#    - Gửi alert khi phát hiện CHoCH
#    - Chạy web dashboard
#
# Cách sử dụng:
# - Để vẽ chart: ENABLE_CHART=1 và CHART_MODE=history
# - Để chạy realtime: ENABLE_CHART=0 (hoặc bất kỳ CHART_MODE nào)

ENABLE_CHART=0
CHART_MODE=realtime

# Chart Configuration (NEW)
# ENABLE_CHART=0  (Default: disabled - không vẽ chart)
# ENABLE_CHART=1  (Enabled - vẽ chart)
ENABLE_CHART=0

# CHART_MODE: 'history' hoặc 'realtime'
# - 'history': Chạy toàn bộ lịch sử, vẽ chart khi phát hiện CHoCH trong quá khứ
# - 'realtime': Chỉ vẽ chart realtime (200 nến gần nhất) cho mỗi chu kỳ
CHART_MODE=realtime

